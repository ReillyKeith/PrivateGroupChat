/*
CONFIG FILES
any and all config files will go here
at the moment it will be used to setup the 
endpoint server that the client talks to
*/

const ENDPOINT = `127.0.0.1:5000`;
module.exports = { ENDPOINT };
